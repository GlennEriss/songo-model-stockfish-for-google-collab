project:
  name: songo-model-stockfish-for-google-collab

runtime:
  device: cpu
  seed: 42

storage:
  drive_root: /content/drive/MyDrive/songo-stockfish
  repo_root: /content/songo-model-stockfish-for-google-collab

job:
  run_type: evaluation
  job_id: auto
  resume: true
  save_every_minutes: 5

evaluation:
  model_id: songo_policy_value_smoke_10
  checkpoint_path: models/final/songo_policy_value_smoke_10.pt
  dataset_id: dataset_v1_smoke_10
  test_dataset_path: data/datasets/dataset_v1_smoke_10/test.npz
  output_dir: reports/evaluations
